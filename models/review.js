/* const mongoose = require('mongoose');
const { Schema, model } = mongoose;

const reviewSchema = new Schema({
    toilet:{
        type:Schema.ObjectId, ref:'user-model'
    }



}) */

/* const ToiletInfo = mongoose.model('Toilet',toiletInfo)
module.exports = ToiletInfo */